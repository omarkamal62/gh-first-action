name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: macos-latest
    steps:
      - name: Print greetings
        run: echo "Hello World!"
      - name: Print goodbye
        run: echo "Done bye"
